name: CD

on:
 push:
   branches:
     - develop

jobs:

 deploy:

   runs-on: ubuntu-latest

   steps:

   - name: Trigger Render

     run:

      curl -X POST "${{ secrets.RENDER_DEPLOY_HOOK_URL }}"