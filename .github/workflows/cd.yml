name: CD Pipeline (No External Deploy)

on:
  push:
    branches:
      - develop

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deployment Simulation
        run: echo "CD SUCCESS - Deployment simulated (no Render used)"